diff --git a/node_modules/three-globe/dist/three-globe.mjs b/node_modules/three-globe/dist/three-globe.mjs
index cc53c4e..264aaaf 100644
--- a/node_modules/three-globe/dist/three-globe.mjs
+++ b/node_modules/three-globe/dist/three-globe.mjs
@@ -688,7 +688,13 @@ var GlobeLayerKapsule = Kapsule({
     var globeMaterial = state.globeObj.material;
 
     // Hide globeObj if it's representing tiles
-    state.tileEngine.visible = !(state.globeObj.visible = !state.globeTileEngineUrl);
+    // Upstream hides the image sphere whenever a tile engine is set. Keep it:
+    // it is the surface under tiles that have not arrived yet (the tile engine
+    // otherwise shows its own black inner sphere there). The app pushes the
+    // sphere a hair behind the tiles with polygonOffset so tiles win the depth
+    // test wherever they exist.
+    state.tileEngine.visible = !!state.globeTileEngineUrl;
+    state.globeObj.visible = true;
     if (changedProps.hasOwnProperty('globeCurvatureResolution')) {
       var _state$globeObj$geome;
       (_state$globeObj$geome = state.globeObj.geometry) === null || _state$globeObj$geome === void 0 || _state$globeObj$geome.dispose();
@@ -1220,7 +1226,12 @@ var ArcsLayerKapsule = Kapsule({
     var tweenGroup = _ref.tweenGroup;
     return {
       tweenGroup: tweenGroup,
-      ticker: new FrameTicker$2(),
+      // Created paused: a FrameTicker starts its rAF loop in its constructor, and
+      // stateInit also runs for the throw-away instance fromKapsule() builds at
+      // module load purely to enumerate method names. That instance is never
+      // init'd or destroyed, so its ticker would spin at display rate forever
+      // with no listeners. init() resumes the ticker for real instances.
+      ticker: new FrameTicker$2(NaN, NaN, true),
       sharedMaterial: new THREE$e.ShaderMaterial(_objectSpread2(_objectSpread2({}, dashedLineShaders()), {}, {
         transparent: true,
         blending: THREE$e.NormalBlending
@@ -1242,7 +1253,8 @@ var ArcsLayerKapsule = Kapsule({
       return obj;
     });
 
-    // Kick-off dash animations
+    // Kick-off dash animations (ticker is created paused in stateInit)
+    state.ticker.resume();
     state.ticker.onTick.add(function (_, timeDelta) {
       state.dataMapper.entries().map(function (_ref2) {
         var _ref3 = _slicedToArray(_ref2, 2),
@@ -2705,7 +2717,8 @@ var PathsLayerKapsule = Kapsule({
     var tweenGroup = _ref.tweenGroup;
     return {
       tweenGroup: tweenGroup,
-      ticker: new FrameTicker$1(),
+      // Created paused — see the arcs layer for why. init() resumes it.
+      ticker: new FrameTicker$1(NaN, NaN, true),
       sharedMaterial: new THREE$8.ShaderMaterial(_objectSpread2(_objectSpread2({}, dashedLineShaders()), {}, {
         transparent: true,
         blending: THREE$8.NormalBlending
@@ -2727,7 +2740,8 @@ var PathsLayerKapsule = Kapsule({
       return obj;
     });
 
-    // Kick-off dash animations
+    // Kick-off dash animations (ticker is created paused in stateInit)
+    state.ticker.resume();
     state.ticker.onTick.add(function (_, timeDelta) {
       state.dataMapper.entries().map(function (_ref2) {
         var _ref3 = _slicedToArray(_ref2, 2),
